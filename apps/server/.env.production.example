# === PRODUCTION ENVIRONMENT VARIABLES ===
# Set these in Render's dashboard under Environment > Environment Variables

# Neon PostgreSQL connection string (get from neon.tech dashboard)
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/moviehive?sslmode=require"

# Server
PORT=4000
NODE_ENV=production
CORS_ORIGIN=https://moviehive.com,https://www.moviehive.com

# JWT secrets (use long random strings - Render can auto-generate these)
JWT_SECRET=generate-a-random-64-char-string-here
JWT_REFRESH_SECRET=generate-another-random-64-char-string-here

# Google OAuth (update callback URL to production)
GOOGLE_CLIENT_ID=265156455711-u2gj4sjp6qv26f01f2k8edasd98rkf4m.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=https://api.moviehive.com/api/auth/google/callback

# Optional: Upload directory (Render provides /tmp or use a persistent disk)
# UPLOAD_DIR=/var/data/uploads
