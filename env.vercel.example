# Paste this into Vercel → Project → Settings → Environment Variables → "Import from .env"
# Replace the value with your real Render API URL (no /api at the end for some setups; check DEPLOYMENT.md).

NEXT_PUBLIC_API_URL=https://moviehive-api.onrender.com/api
